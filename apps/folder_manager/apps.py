from django.apps import AppConfig


class FolderManagerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.folder_manager'
    verbose_name = 'Folder Manager'

